# Loot Goblin prototype context

Skeleton Archer reference follow-up (2026-09-17): the user supplied the omitted concept. Two bounded visual iterations changed only SkeletonArcherRig presentation and documentation: beveled oversized skull, lighter ivory, tapered rib cage, red waist cloth, red quiver fletching, chunkier limb/bow geometry and a small head-angle adjustment. The procedural rig topology, state machine, hand targets, timing and all gameplay systems remain intact. Both graphics-enabled Archer validation runs passed all 49 checks; final actual-camera poses were inspected. See Docs/SkeletonArcher.md for details and before/after evidence paths.

Skeleton Archer addition (2026-09-17): procedural Enemy #2 is generated by SkeletonArcherRig, driven by SkeletonArcher's explicit Reposition/DrawArrow/Aim/Fire/Recovery state machine, and uses SkeletonArrow for non-homing swept planar projectiles. LootGoblinRun retains health, sword targeting/damage, loot and room accounting. Spawn slots 1 and 5 become Archers without changing the existing room count or eight-enemy cap. Persistent tuning is on the scene's LootGoblinRun > Skeleton Archer settings. See `Docs/SkeletonArcher.md` for axes, hierarchy, timings and evidence. The dedicated 49-check graphics-enabled Play Mode suite and ten-room/59-loot depth suite pass. Gameplay-camera reload/aim/fire/recoil and multiple facing directions were visually inspected. The legacy full smoke suite has a stale Slime assertion (0.3-second test wait versus unchanged 0.8-second prefab windup); failure evidence is preserved in Logs/ArcherLegacySmokeResult.txt. No target-device profiling or new WebGL build was performed for this addition.
Unity 6000.6.0f1, URP 17.6.0, Input System 1.20.0 (new input only). Fresh template; no existing gameplay. Existing Player/Move action is reused. No packages added. Unity MCP currently targets another project (Dominion); do not use it to mutate this project.

Scene: Assets/Scenes/LootGoblin.unity. One runtime component builds on serialized primitive geometry; it owns movement, simple enemy steering, stop-to-attack, loot and five room resets. PlayerHealth owns player damage. R restarts. The runtime Tick(Vector2,float) is also the bounded smoke-test entry point.

Slime addition (2026-09-15): LootGoblinRun loads Assets/Prototype/Resources/Slime.prefab once at startup and uses it for all existing room spawns. SlimeMotion supplies grounded hop timing, idle wobble, a squash wind-up, teardrop stretch-lunge, recovery and a 0.32-second death collapse. Player hits can interrupt an attack and knock the slime back, while the slime cannot cancel its own committed attack. The run retains steering, targeting, loot and progression ownership. Dying enemies cannot be targeted and remain counted until collapse completes. The original capsule is retained as a fallback if the prefab is absent.

Player health addition (2026-09-15): PlayerHealth is attached to the runtime-built player and owns 100 HP, a 0.75-second post-hit invulnerability window, and a small scale punch. The Slime lunge exposes a one-tick attack contact; LootGoblinRun applies 25 damage only when that lunge is within contact distance. Health persists across normal rooms, while Restart restores full health. At zero health the run enters Failed, freezes Tick processing, and the existing R restart path restores the run. The IMGUI debug HUD displays current health and RUN FAILED when appropriate.

Validation: Loot Goblin/Run Smoke Test passed with slime animation checks and the existing complete five-room/20-loot/restart checks. Logs/LootGoblinValidation.txt contains results; Logs/LootGoblinPortrait.png shows visibility from the gameplay camera. Unity compiled successfully. GameObjectInspector MissingReferenceException/SerializedObjectNotCreatableException messages appeared during script reload; their cause is unresolved. The baseline separately contained player-reference and rig errors. No slime runtime errors occurred in the passing smoke test. No player build or device test was run.

Baseline on 2026-09-15: ProjectSettings/ProjectSettings.asset and QualitySettings.asset already modified by the user; preserve both. SampleScene and template assets are preserved. EditorBuildSettings is changed only to make LootGoblin the playable build scene.

Arena polish pass (2026-09-16): Assets/Scenes/LootGoblin.unity contains a deterministic `Arena Environment` hierarchy. The floor is one continuous 10.8-by-15.6 URP-lit plane using `Assets/Prototype/Textures/ArenaFloorStone.jpg`; its material center-crops the square source through UV scale/offset so the stones retain their proportions without visible tile repetition. The perimeter uses meshes and the material in `Assets/Environment_Asset_Pack 1.fbx`: 26 aligned wall pieces, four corner pillars, and a centered north gateway with its separate gate. Five stone obstacles form two single blocks and one three-block L cluster.

Arena collision contract: the serialized BoxColliders on the visible wall, pillar, gate, and obstacle objects are authoritative. LootGoblinRun receives the five visible obstacle colliders and uses their bounds for player resolution, enemy movement, and enemy line-of-sight. Do not reintroduce the former hard-coded circular `Pillars` blockers. The gateway arch has no opening-spanning collider; the centered gate alone blocks the exit while a room is active and is hidden after room clear. `Loot Goblin/Apply Arena Polish` can rebuild this scene layout deterministically if the environment needs to be regenerated.

Arena validation (2026-09-16): the automated portrait smoke suite passed all five rooms, 20 total loot, movement/input bindings, combat, visible-obstacle collision, the 63-tile floor, perimeter colliders, gate closed/open transitions, portrait camera sizes, and restart. Unity's open editor compiled and reloaded the changed assemblies successfully. Evidence is in `Logs/LootGoblinValidation.txt` and `Logs/LootGoblinPortrait.png`. A clean WebGL player build was also run from an isolated validation copy; see `Logs/ArenaWebGLValidation.log` for its final result. No physical phone/browser-device session was run.

